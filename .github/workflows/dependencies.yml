name: Dependecies Example
on:
  workflow_dispatch

jobs:
  dependecies-example:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v5.0.1
      - uses: actions/setup-node@v4.0.0
        with:
          node-version: '18'
      - run: ls -alhR
      
    
